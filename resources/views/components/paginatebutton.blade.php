<div class="row" style="justify-content: center">
    <a href="{{ $prev }}" disabled class="btn btn-primary mr-2 ml-2 {{ $prev == '' ? 'disabled' : '' }}">Back</a>
    <a href="{{ $next }}" class="btn btn-primary mr-2 ml-2 {{ $next == '' ? 'disabled' : '' }}">Next</a>
</div>
